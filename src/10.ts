// *** 一、什麼是型別推斷？ ***

// *** 二、TypeScript 會從哪裡推斷型別？ ***

// *** 三、從初始化值推斷 ***

// *** 四、函式回傳值也會被推斷 ***

// *** 五、`let` 和 `const` 的推斷差異 ***

// *** 六、物件推斷與 `as const` ***

// *** 七、`as Type` 與 `!` 非空斷言 ***

// *** 八、陣列、Contextual Typing 與常見推斷限制 ***

// *** 九、什麼時候依賴推斷，什麼時候手寫型別？ ***
